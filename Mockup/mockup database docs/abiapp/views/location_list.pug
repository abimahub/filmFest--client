extends layout

block content
  h1= title
  
  ul
    each location in location_list
      li 
        a(href=location.url) #{location.name} 
        |  (#{location.lifespan})

    else
      li There are no locations.